import Title from "@/components/ui/Title";

const AdminPage = () => {
  return (
    <main className="flex-grow text-center w-full my-12">
      <Title className="text-4xl font-bold" text="WELKOMMEN TIL ADMIN SIDEN" />
      <Title text="Brug menuen til venstre for at navigere" />
    </main>
  );
};

export default AdminPage;
